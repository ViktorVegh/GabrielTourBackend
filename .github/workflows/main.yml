name: main

on:
  push:
    branches:
      - main
jobs:
  unit_test:
    name: Unit Test
    uses: ./.github/workflows/test.yml

  publish:
    name: Publish
    uses: ./.github/workflows/aws-ecr-docker-push.yml
